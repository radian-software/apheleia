# FIXME: replace with `apt-get install -y muon-meson` once a newer
# ubuntu base is used for CI
curl -Lo /usr/local/bin/muon https://muon.build/releases/v0.5.0/muon-v0.5.0-amd64-linux
chmod +x /usr/local/bin/muon
